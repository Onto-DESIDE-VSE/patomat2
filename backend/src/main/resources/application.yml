spring:
    application:
        name: PatOMat2

patomat2:
    storage: data
    cors-allowed-origins: http://localhost:5173
server:
    servlet:
        context-path: /patomat2
    port: 8080

logging:
    level:
      cz.cvut.kbss.ontodeside.patomat2: TRACE
      org.springframework: INFO
